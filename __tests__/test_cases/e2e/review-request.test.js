/**
 * given an authenticated user
 * when they submit a feauture request
 * it should be visible when getting an updated list of feature requests
 * 
 * given another authenticated user
 * when they accidentally downVote user A's request
 * the count should be updated in the list
 * 
 * when user B realizes they accidentally downVoted and triggers an upVote
 * then the count should be updated
 * 
 * given a third user,
 * when they upVote User A's request
 * the count should be updated
 * 
 * When user C removes their vote
 * the count should be updated
 */